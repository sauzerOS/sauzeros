#!/bin/sh -e

meson setup build         \
      --prefix=/usr       \
      --buildtype=release \
      -D others=enabled   \
      -D man=false        \
      -D glycin=disabled  \
      --wrap-mode=nofallback

meson compile -C build
DESTDIR=$1 meson install -C build
