#!/bin/sh -e

meson setup build               \
      --prefix=/usr             \
      --buildtype=release       \
      -D documentation=disabled \
      -D tests=disabled

meson compile -C build
meson install -C build --destdir=$1
