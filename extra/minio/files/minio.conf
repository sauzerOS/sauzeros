# Local export path.
MINIO_VOLUMES="/srv/minio/data/"
# Server user.
MINIO_ROOT_USER=example-user
# Server password.
MINIO_ROOT_PASSWORD=example-password
# Use if you want to run Minio on a custom port. compat is for ntfs fs
MINIO_OPTS="--address localhost:7777 --console-address localhost:33333 --compat"
