#!/bin/sh -e

CFLAGS="$CFLAGS -fno-lto"
CXXFLAGS=$CFLAGS

./configure          \
    --prefix=/usr    \
    --shared-libuv   \
    --shared-nghttp2 \
    --shared-zlib    \
    --shared-openssl \
    --shared-brotli  \
    --with-intl=system-icu

make -j$(( $(nproc) / 2 ))
make DESTDIR=$1 install
