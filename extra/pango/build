#!/bin/sh -e

meson setup build        \
  --prefix=/usr          \
  --buildtype=release    \
  --wrap-mode=nofallback \
  -D introspection=enabled

meson compile -C build
meson install -C build --destdir=$1
