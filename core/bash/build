#!/bin/sh -e

export CFLAGS="$CFLAGS -DSYS_BASHRC='\"/etc/bash/bashrc\"'"

patch -Np0 -i bash53-001
patch -Np0 -i bash53-002
patch -Np0 -i bash53-003

./configure \
    --prefix=/usr \
    --with-installed-readline

make
make DESTDIR="$1" install


ln -s bash "$1/usr/bin/sh"

