#!/bin/sh -e

mkdir build
cd    build

meson setup ..                  \
      --prefix=/usr             \
      --buildtype=release       \
      -D glib_debug=disabled    \
      -D man-pages=disabled     \
      -D sysprof=disabled

ninja
DESTDIR=$1 ninja install
